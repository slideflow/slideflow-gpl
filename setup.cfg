[build-system]
requires = ["setuptools", "versioneer[toml]"]
build-backend = "setuptools.build_meta"

[versioneer]
VCS = git
style = pep440
versionfile_source = slideflo_extras/_version.py
versionfile_build = slideflow_extras/_version.py
tag_prefix = 
parentdir_prefix = slideflow_extras-
